// EXPECT_ERROR Operator overload <__add__> must take exactly 2 parameters
struct Value
    number: int
    function __add__(self): int
        return self.number
    end function
end struct
on init
    declare v := Value(5)
    message(v + 1)
end on
