// EXPECT_ERROR Operator overload <__add__> must return exactly 1 value
struct Value
    number: int
    function __add__(self, other: int)
        message(self.number + other)
    end function
end struct
on init
    declare v := Value(5)
    message(v + 1)
end on
