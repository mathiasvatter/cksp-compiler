FEATURES:
For a more extensive collection of features and an introduction into the syntax of CKSP, please refer to the online documentation:
https://mathiasvatter.github.io/cksp-compiler/

KNOWN ISSUES (in comparison to the sublime ksp fork):
 - <import as> namespaces
 - concat()

NOT SUPPORTED, BUT DIAGNOSED (the compiler names these and says what replaces them):
 - declare list, list_add()            -> list blocks, or fixed-size arrays with indexed assignments
 - taskfunc, tcm.init(), tcm.wait()   -> plain functions; a quick fix rewrites the block
 - property blocks                    -> a pair of struct methods, or a define
 - iterate_post_macro(), literate_post_macro()
 - {#pragma ...}                      -> the cksp #pragma spelling; a quick fix rewrites it

LANGUAGE SERVER:
Run <cksp --lsp> to serve diagnostics, navigation, completion and quick fixes over stdio.
Editors other than VS Code can integrate against it; the protocol contract is documented at:
https://mathiasvatter.github.io/cksp-compiler/language-server/

PREPROCESSOR ORDER:
 - Tokenizer:
     - Comments are removed.
     - Line continuations <…> are joined
 - Imports are handled by recursively executing the Tokenizer
 - <USE_CODE_IF/IF_NOT> is executed
 - Preprocessor Parsing to PreAST
 - PreAST lowering:
     - defines are substituted
     - <START_INC> directive is executed
     - iterate_macro and literate_macro are expanded
     - macros are substituted


